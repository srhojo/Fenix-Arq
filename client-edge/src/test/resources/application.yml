server:
  port: 0
  
eureka:
  instance:
    appname: client-test
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

com:
  fenix:
    client-test:
      title: ClientTest
    test:
      texto: Hola Javi

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic